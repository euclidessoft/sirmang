{% extends 'layout4.html.twig' %}

{% block activite %} class="active" {% endblock %}

{% block body %}
<h3><i class="fa fa-suitcase"></i> Caisse /  Activit&eacute;s
    <a  class="btn btn-success pull-right mr-3" href="{{ path('rubrique_index') }}"><i class="fa fa-list"></i> Rubriques</a>

</h3>
<div class="row"  style="border-bottom: 1px solid #005E89;">
    <div class="col-12">
        <h4 style="display: inline;">{{ activite.date|date('d/m/Y') }} : {{ activite.nom }} &agrave; {{ activite.Lieu }}</h4>
        <a  class="btn btn-primary pull-right mb-2" href="{{ path('activite_show', { 'id' : activite.id})}}"><i class="fa fa-undo"></i> Retour</a>
    </div>
    </div>
        
    <div class="row">
 
        <div class="col-lg-12">
            <div class="col-lg-6 offset-lg-3 mt-5 mb-5 pb-1 fond-inner">
                <h4 class="mt-4 mb-5 text-bold">
                    {{ 'Nouvelle Depense'|trans }}
                </h4>
    
        {{ include('activite/_form.html.twig') }}
        
    
    </div>
    </div>
    </div>
    
{% endblock %}
