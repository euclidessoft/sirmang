{% extends 'layout4.html.twig' %}

{% block activite %} class="active" {% endblock %}

{% block body %}
<h3><i class="fa fa-suitcase"></i> Caisse /  Activite</h3>
<div class="row">
    <div class="col-12"><a  class="btn btn-primary pull-right mb-3" href="{{ path('activite_show', { 'id' : activite.id})}}">Retour</a></div>
    </div>
        <div class="activite">
            
        <table width="100%" class="table table-success-light table-bordered table-hover table-striped">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Lieu</th>
                </tr>
            </thead>
            <tbody>
            
                <tr>
                    <td>{{ activite.date|date('d/m/Y') }}</td>
                    <td>{{ activite.Lieu }}</td>
                </tr>
            </tbody>
        </table>
    </div>
    <div class="row">
 
        <div class="col-lg-12">
            <div class="col-lg-6 offset-lg-3 mt-5 mb-5 pb-1 fond-inner">
                <h4 class="mt-4 mb-5 text-bold">
                    {{ 'Modification Depense'|trans }}
                </h4>
    
        {{ include('activite/_form.html.twig') }}
    
    </div>
    </div>
    </div>
{% endblock %}
