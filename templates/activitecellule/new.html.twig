{% extends 'layout4.html.twig' %}

{% block title %}Nouvelle Zonne{% endblock %}
{% block activite %} class="active" {% endblock %}

{% block body %}
<h3><i class="fa fa-suitcase"></i> Caisse / {{ app.user.cellule.nom }}  /Activites</h3>
<div class="row">
    <div class="col-12 text-right mt-3">  
          <a  class="btn btn-primary" href="{{ path('activitecellule_index', {'cellule' : app.user.cellule.id}) }}">Retour</a>
</div>
</div>
<div class="row">
 
    <div class="col-lg-12">
        <div class="col-lg-6 offset-lg-3 mt-5 mb-5 pb-1 fond-inner">
            <h4 class="mt-4 mb-5 text-bold">
                {{ 'Nouvelle Activite'|trans }}
            </h4>

    {{ include('activite/_form.html.twig') }}

</div>
</div>
</div>
{% endblock %}
