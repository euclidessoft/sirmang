<form method="post" action="{{ path('autreentree_delete', {'id': autreentree.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer?');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autreentree.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i> Supprimer</button>
</form>
