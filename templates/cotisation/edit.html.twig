{% extends 'layout2.html.twig' %}

{% block title %}Edit Cotisation{% endblock %}

{% block body %}
<div class="row">
    <div class="col-12 text-right mt-3">  
          <a  class="btn btn-primary" href="{{ path('security_profile') }}">Retour</a>
</div>
</div>
<div class="row">
    
    <div class="col-lg-12">
        <div class="col-lg-6 offset-lg-3 mt-5 mb-5 pb-1 fond-inner">
            <h4 class="mt-4 mb-5 text-bold">
                {{ 'Modification Cotisation'|trans }} 
            </h4>

    {{ include('cotisation/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    </div>
    </div>
    
{% endblock %}
