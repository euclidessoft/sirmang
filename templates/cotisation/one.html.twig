{% extends 'layout3.html.twig' %}

{% block title %}Cotisation index{% endblock %}
{% block cotisation %} class="active"{% endblock %}

{% block body %}
    <h3><i class="fa fa-suitcase"></i> Caisse /  {{ cellule.nom }} / Cotisations</h3>
    <h3 class="text-center">{{ annee.nom }}</h3>
    <div class="row">
        <div class="col-12 texte-right">

            <a  class="btn btn-primary" href="{{ path('cotisation_cellule', { cellule : cellule.id, mois : 1}) }}" ><i class="fa fa-users"></i> Cotisation Regroupee</a>
        </div>
    </div>
    <div class="row">


    </div>
    <div class="col-lg-12">
        <div class="col-lg-6 offset-lg-3 mt-5 mb-5 pb-1 fond-inner">
            <h4 class="mt-4 mb-5 text-bold">
                {{ 'Cotisation'|trans }}
            </h4>

            {{ include('cotisation/_form.html.twig') }}

        </div>
    </div>

{% endblock %}

