{% extends 'layout2.html.twig' %}

{% block title %}Cotisation{% endblock %}

{% block body %}
    <h1>Cotisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cotisation.id }}</td>
            </tr>
            <tr>
                <th>Professionnel</th>
                <td>{{ cotisation.professionnel }}</td>
            </tr>
            <tr>
                <th>Niveau1</th>
                <td>{{ cotisation.niveau1 }}</td>
            </tr>
            <tr>
                <th>Niveau2</th>
                <td>{{ cotisation.niveau2 }}</td>
            </tr>
            <tr>
                <th>Niveau3</th>
                <td>{{ cotisation.niveau3 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cotisation_index') }}">back to list</a>

    <a href="{{ path('cotisation_edit', {'id': cotisation.id}) }}">edit</a>

    {{ include('cotisation/_delete_form.html.twig') }}
{% endblock %}
