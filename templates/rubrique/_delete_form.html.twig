<form method="post" action="{{ path('rubrique_delete', {'id': rubrique.id}) }}" onsubmit="return  onsubmit="return confirm('Voulez- vous vraiment supprimer la rubrique?');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rubrique.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i> Supprimer</button>
</form>
